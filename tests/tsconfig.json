{
  "compilerOptions": {
    "target": "es2017",
    "module": "commonjs",
    "baseUrl": "./", // This must be specified if "paths" is.
    "paths": {
      "@helpers/*": ["../helpers/*"],
      "@constants/*": ["../constants/*"] // This mapping is relative to "baseUrl".
    }
  }
}